<% provide(:title, 'All users') %>
<h1>All users</h1>


<table class=" users table table-hover">
    <thead>
      <tr>
        <th>Username</th>
        <th>Email</th>
      </tr>
    </thead>
    <tbody>
        <%= render @users %>
         <!-- this used to be:
         <% @users.each do |user| %>
           <%= render user %>
         <% end %>
          but rails magic!
        -->
    </tbody>
  </table>
